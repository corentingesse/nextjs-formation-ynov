import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Recherche",
  description: "Recherchez des sites web par tags",
};

export default function SearchPage() {
  return <main>SearchPage</main>;
}
